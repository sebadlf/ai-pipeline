services:
  postgres:
    image: timescale/timescaledb:latest-pg16
    container_name: trading-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-trading}
      POSTGRES_USER: ${POSTGRES_USER:-trading}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-changeme}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
    networks:
      - ml-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-trading}"]
      interval: 5s
      timeout: 5s
      retries: 5

  mlflow:
    image: ghcr.io/mlflow/mlflow:v3.10.1
    container_name: trading-mlflow
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      MLFLOW_BACKEND_STORE_URI: postgresql://${POSTGRES_USER:-trading}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/${POSTGRES_DB:-trading}
      MLFLOW_DEFAULT_ARTIFACT_ROOT: mlflow-artifacts:/mlruns
    ports:
      - "5000:5000"
    volumes:
      - ./mlruns:/mlruns
    command: >
      bash -c "pip install psycopg2-binary &&
      mlflow server
      --host 0.0.0.0
      --port 5000
      --backend-store-uri postgresql://${POSTGRES_USER:-trading}:${POSTGRES_PASSWORD:-changeme}@postgres:5432/${POSTGRES_DB:-trading}
      --default-artifact-root mlflow-artifacts:/mlruns
      --artifacts-destination /mlruns
      --serve-artifacts"
    networks:
      - ml-network

networks:
  ml-network:
    name: ml-network
