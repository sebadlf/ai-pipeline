---
tags: [knowledge, python]
---

# Python — Base de conocimiento

## Tooling

- [[uv]] — package manager y virtual env manager (reemplazo de pip + venv + pyenv)
- [[Ruff]] — linter + formatter (reemplazo de flake8 + black + isort)

## Patrones

- [[Polars sobre pandas]] — por qué Polars para pipelines grandes en RAM limitada
- [[Multiprocessing con spawn para MPS]] — por qué `spawn` context y no fork cuando hay PyTorch MPS

## Por explorar

- [ ] Type checking con mypy (opcional en CI)
- [ ] Structured logging con structlog
