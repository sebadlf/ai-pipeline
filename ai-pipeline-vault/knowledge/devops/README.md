---
tags: [knowledge, devops]
---

# DevOps — Base de conocimiento

## CI/CD

- [[GitHub Actions — ruff + pytest]] — workflow básico de CI
- [[Auto-merge con squash]] — `gh pr merge --auto --squash` cuando CI pasa
- [[Pre-commit hooks]] — configuración estándar para proyectos Python

## Infraestructura local

- [[Docker Compose — Postgres + MLflow]] — patrón de stack de dev
- [[TimescaleDB setup]] — extensión, hypertables, init scripts
- [[MPS en macOS]] — por qué PyTorch nativo y no dentro de Docker en Apple Silicon

## Secrets

- [[.env management]] — python-dotenv, .env.example, no commitear
- [[FMP_API_KEY y rate limits]] — cómo manejarlos en ingestion

## Monitoring

- [[MLflow UI]] — qué mirar, cómo comparar runs
- [[Logging patterns]] — structured logging, niveles, qué loggear
