---
date: 2026-04-18
tags: [daily]
---

# Daily Log — 2026-04-18

## Foco del día
- [x] Integrar Claude Code + Obsidian + Linear + GitHub en este repo

## Lo que hice

- Scaffolding del vault `ai-pipeline-vault/` (HOME, projects/ai-pipeline, knowledge, workflows, daily-logs, retrospectives) con los 6 templates estándar.
- GitHub Actions: `ci.yml` (ruff + pytest) y `auto-merge.yml` (squash auto-merge cuando CI pasa). PR template con slot de Linear ID.
- Extendí `CLAUDE.md` con secciones External References, Git Conventions y Workflow. Confirmado team Linear `Becerra` (prefix `BEC`).
- Seed del vault con 3 ADRs (Docker híbrido, Polars, Ensemble top-3), 6 domain notes (features, normalization, clustering, Optuna, promotion, regime/backtest) y 3 runbooks (setup, pipeline end-to-end, MLflow cleanup).
- Permisos MCP (`mcp__linear__*`, `mcp__obsidian__*`, `mcp__github__*`) + git/gh bash helpers en `.claude/settings.local.json`.

## Aprendizajes

- El `knowledge/ml/` dedicado es esencial en este vault por la densidad de conocimiento ML transversal (Optuna, feature engineering, regime detection, backtesting).
- CLAUDE.md ya era extenso (~28KB) — extenderlo sin reescribir mantiene la densidad técnica y agrega sólo lo de integración.
- No se incluyó `release.yml`: el proyecto no se distribuye.

## Blockers / Preguntas

- **Ninguno**. El team Linear BEC ya existe.

## Notas para mañana

- Primer issue real en Linear (BEC-...) para testear end-to-end el flujo: crear issue → branch → PR → auto-merge → update vault.
- Agregar `.gitignore` entry para `ai-pipeline-vault/.obsidian/` cuando se abra el vault en Obsidian (se crea configuración local que no va al repo).
